let objects = ["pencil", "book", "eraser"];

objects.push("pen");
console.log(objects);

objects.pop();
console.log(objects);

// push() adds an item to the end.
// pop() removes the last item.
